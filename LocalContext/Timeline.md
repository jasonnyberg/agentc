### 2026-05-19
- Advanced 🔗[G091 — Intern Worker Concurrency MVP](./Knowledge/Goals/G091-InternWorkerConcurrencyMvp/index.md) with first worker-visible cooperative cancellation checkpoints: async worker jobs carry a cancellation token into `runInternWorker`, and worker `yield!` boundaries now check that token before `EdictVM::resume()` executes later program steps.
- Advanced 🔗[G110 — Root1 eventfd/epoll Resource Broker and Micro-VM IPC Design](./Knowledge/Goals/G110-EventfdEpollMicroVmIpcDesign/index.md) by documenting the public parking `await!` contract boundary over continuation handles/status: resumed code receives a structured envelope, timeout/cancel are terminal envelopes, handles are retained until drop, and durable continuation identity is deferred to G096/G104.

### 2026-05-18
- Advanced 🔗[G110 — Root1 eventfd/epoll Resource Broker and Micro-VM IPC Design](./Knowledge/Goals/G110-EventfdEpollMicroVmIpcDesign/index.md) again by replacing the raw `EdictVM*` parking record in `Root1AwaitScheduler` with logical continuation handles/status, callback-backed VM resumption, descriptor retrieval by handle, cancellation, timeout, and explicit drop semantics.
- Advanced 🔗[G110 — Root1 eventfd/epoll Resource Broker and Micro-VM IPC Design](./Knowledge/Goals/G110-EventfdEpollMicroVmIpcDesign/index.md) again with the first await parking table prototype: `Root1AwaitScheduler` parks yielded VMs by logical participant waitable, pushes descriptor events into resumed VM stacks, and resumes via `EdictVM::resume()` when broker polling reports readiness.
- Advanced 🔗[G110 — Root1 eventfd/epoll Resource Broker and Micro-VM IPC Design](./Knowledge/Goals/G110-EventfdEpollMicroVmIpcDesign/index.md) again with the first VM hook for continuation parking: `EdictVM::resume()` now continues an existing yielded code frame, with regression coverage proving execution resumes after `yield!`.
- Advanced 🔗[G110 — Root1 eventfd/epoll Resource Broker and Micro-VM IPC Design](./Knowledge/Goals/G110-EventfdEpollMicroVmIpcDesign/index.md) again by exposing logical resource/lease controls through Root1 primitives and `root1.edict`: resource create/acquire/release, lease register, heartbeat, and recover-expired now operate on logical handles and are covered by `Root1PrimitiveModuleTest.*`.
- Advanced 🔗[G110 — Root1 eventfd/epoll Resource Broker and Micro-VM IPC Design](./Knowledge/Goals/G110-EventfdEpollMicroVmIpcDesign/index.md) again with participant-level lease recovery: `heartbeatParticipant` renews all valid owner leases, `recoverParticipantLeases` recovers resources owned by a participant, and pidfd owner-death polling now recovers registered leases for the dead participant; `Root1ResourceBrokerTest.*` now passes 18/18.
- Advanced 🔗[G110 — Root1 eventfd/epoll Resource Broker and Micro-VM IPC Design](./Knowledge/Goals/G110-EventfdEpollMicroVmIpcDesign/index.md) again with deterministic lease/stale-owner recovery for registered resources: `registerLease`, `renewLease`, and `recoverExpiredLeases` now cover expired recovery to a waiter, renewed lease non-recovery, and mismatched-owner rejection; `Root1ResourceBrokerTest.*` passed 15/15.
- Checkpointed HRM state after the G091/G099/G110 slices: Dashboard, active goals, intern-worker fact, and Handoff Note now point next to G110 lease/stale-owner recovery and continuation-parking `await!` design.
- Advanced 🔗[G110 — Root1 eventfd/epoll Resource Broker and Micro-VM IPC Design](./Knowledge/Goals/G110-EventfdEpollMicroVmIpcDesign/index.md): added `Root1ResourceBroker::recoverAbandonedResource(...)` for known abandoned owner/resource recovery, including waiter grant and `OwnerDied` descriptors, and added module-backed `root1.await!` as a non-parking logical-waitable poll/drain helper; `Root1ResourceBrokerTest.*` passes 12/12 and `Root1PrimitiveModuleTest.*` passes 1/1.
- Advanced 🔗[G091 — Intern Worker Concurrency MVP](./Knowledge/Goals/G091-InternWorkerConcurrencyMvp/index.md) lifecycle cleanup: async terminal statuses are now retained for repeated sync until explicit `worker.edict_drop!` or bounded sweep, active count excludes terminal/abandoned jobs, running drops return `state: "abandoned"` and suppress later orphan mailbox publication, terminal drops return `state: "dropped"`, and cancellation is non-retroactive after terminal completion. Started 🔗[G099 — Intern Task Quality Contracts](./Knowledge/Goals/G099-InternTaskQualityContracts/index.md) with Edict-level `intern.validate_task_contract!` / `intern.validate_status_envelope!` helpers and regression coverage; `InternWorkerTest.*:Root1PrimitiveModuleTest.*` passes 12/12 and the focused Edict slice passes 58/58.

### 2026-05-17
- Completed 🔗[G111 — Root1/Worker Primitive FFI and Edict Intern Surface Migration](./Knowledge/Goals/G111-Root1WorkerPrimitiveFfiEdictInternMigration/index.md): added run-status/start-status worker primitives, moved public run/start envelope construction into `intern.edict`, removed envelope-returning raw worker words plus redundant raw check-capacity from the imported module/header surface, deleted the legacy native run/start/collect/sync/cancel service path, renamed the transitional native implementation from `edict_vm_intern.cpp` to `edict_worker_primitives.cpp`, split fresh worker-VM execution into `edict_worker_runtime.{h,cpp}`, and split thin C ABI/LTV wrappers into `edict_worker_primitives_ffi.cpp`; focused `InternWorkerTest.*:Root1PrimitiveModuleTest.*` remains 10/10, the focused Edict slice remains 56/56, `reflect_tests` remains 43/43, and `cpp_agent_tests` remains 49/49.
- Advanced 🔗[G111 — Root1/Worker Primitive FFI and Edict Intern Surface Migration](./Knowledge/Goals/G111-Root1WorkerPrimitiveFfiEdictInternMigration/index.md): deleted `VMOP_INTERN_RUN`, `VMOP_INTERN_START`, and `VMOP_INTERN_SYNC` from VM enum/dispatch/bootstrap, ported public intern tests to module imports, added coverage proving raw VM startup no longer installs intern builtins, and updated README/K032/Edict reference docs; focused `InternWorkerTest.*:Root1PrimitiveModuleTest.*` is now 10/10, the focused Edict slice is 56/56, `reflect_tests` is 43/43, and `cpp_agent_tests` is 49/49.
- Advanced 🔗[G111 — Root1/Worker Primitive FFI and Edict Intern Surface Migration](./Knowledge/Goals/G111-Root1WorkerPrimitiveFfiEdictInternMigration/index.md): added `worker.edict_collect_status!` and moved module-backed async `intern_sync!` / `intern_cancel!` public envelope construction for running, cancel-requested, complete/error, cancelled, and unknown-job states into `intern.edict`; focused `InternWorkerTest.*:Root1PrimitiveModuleTest.*` remains 9/9, the focused Edict slice with Root1 coverage remains 55/55, `reflect_tests` passes 43/43, and `cpp_agent_tests` passes 49/49.
- Advanced 🔗[G111 — Root1/Worker Primitive FFI and Edict Intern Surface Migration](./Knowledge/Goals/G111-Root1WorkerPrimitiveFfiEdictInternMigration/index.md): added `worker.edict_capacity_status!`, moved public backpressure-envelope construction into `intern.edict`, added importable Root1 LTV primitives plus `root1.edict` wrappers for participant registration, polling, mailbox send/drain, cancellation, and backpressure descriptors, and added `Root1PrimitiveModuleTest` coverage; `InternWorkerTest.*:Root1PrimitiveModuleTest.*` now passes 9/9, the focused Edict slice with Root1 coverage passes 55/55, `reflect_tests` passes 43/43, and `cpp_agent_tests` passes 49/49.
- Began implementing 🔗[G111 — Root1/Worker Primitive FFI and Edict Intern Surface Migration](./Knowledge/Goals/G111-Root1WorkerPrimitiveFfiEdictInternMigration/index.md): added an `agentc::edict::intern` service boundary, importable LTV worker primitives (`agentc_worker_edict_*_ltv`), `worker.edict` / `intern.edict` module wrappers, split module-backed `intern_run!` into prepare/run-prepared, `intern_start!` into prepare/capacity/start-prepared, `intern_sync!` into drain/collect, and `intern_cancel!` into request-cancel/collect composition; regression coverage proved module-backed intern words, active-count, direct worker prepare/capacity/drain/collect/request-cancel, module backpressure branching, and explicit drop cleanup, with `InternWorkerTest.*` 8/8 and the focused Edict slice 54/54 passing.
- Added 🔗[G111 — Root1/Worker Primitive FFI and Edict Intern Surface Migration](./Knowledge/Goals/G111-Root1WorkerPrimitiveFfiEdictInternMigration/index.md): the plan is to keep only irreducibly native eventfd/epoll/pidfd/mmap/thread/fresh-worker-VM mechanisms in importable C/C++ primitive libraries while moving `intern_run!`, `intern_start!`, `intern_sync!`, and cancellation/backpressure/envelope policy into plain Edict modules.

### 2026-05-16
- Advanced 🔗[G091 — Intern Worker Concurrency MVP](./Knowledge/Goals/G091-InternWorkerConcurrencyMvp/index.md) again: added cooperative raw Edict `intern_cancel!` as a plain bootstrap word over `intern_sync!` rather than a new VM opcode, `max_active_jobs` async backpressure, broker-style `Cancelled`/`Backpressure` descriptors, and focused coverage now passing `InternWorkerTest.*` 7/7 plus focused Edict 53/53.
- Completed 🔗[G109 — Listree ReadOnly Mutation Surface Hardening](./Knowledge/Goals/G109-ListreeReadOnlyMutationSurfaceHardening/index.md): added parent-aware Listree item-history mutation helpers, taught `Cursor` to track owning parents and refuse read-only assignment/removal/remove-head/cleanup pops, added VM/Cursor/Listree/G091 shared-context regressions, and validated listree 71/71, reflect 43/43, focused Edict 50/50, and cpp-agent 49/49.
- Continued implementing 🔗[G110 — Root1 eventfd/epoll Resource Broker and Micro-VM IPC Design](./Knowledge/Goals/G110-EventfdEpollMicroVmIpcDesign/index.md): added fixed-size `MailboxDescriptor`, bounded `MailboxRing`, descriptor event kinds/payload handles, descriptor wake/drain APIs, file-backed/anonymous `CoordinationSlab` placement for participant mailboxes and resource state words, mapped-mailbox/remap coverage, fd reconstruction after remap, cancellation/backpressure helpers, and first pidfd owner-death reporting; `Root1ResourceBrokerTest.*` now passes 10/10 and full `reflect_tests` passes 43/43.
- Advanced 🔗[G091 — Intern Worker Concurrency MVP](./Knowledge/Goals/G091-InternWorkerConcurrencyMvp/index.md): added broker-compatible raw Edict `intern_start!` / `intern_sync!` backed by an Edict-local `InternJobManager` and G110 descriptor/waitable shape, keeping `intern_run!` as blocking sugar; `InternWorkerTest.*` now passes 4/4 and `edict_tests` / `cpp_agent_tests` build successfully.
- Reframed and reprioritized 🔗[G110 — Root1 eventfd/epoll Resource Broker and Micro-VM IPC Design](./Knowledge/Goals/G110-EventfdEpollMicroVmIpcDesign/index.md) as the front-loaded synchronization substrate for the micro-VM plan: per-participant eventfds plus a Root1 epoll/parking-lot broker keyed by slab `ResourceKey`s replace per-item eventfds, while ownership-requested/contended flags, grant tokens, mailbox descriptors, and fd-rebuild-on-resume semantics shape G091 async interns, G106 publication registry, and G107 process-isolated workers.

### 2026-05-14
- Added 🔗[WP — Listree Traversal State and ReadOnly Slab Audit](./Knowledge/WorkProducts/WP-ListreeTraversalStateReadOnlySlabAudit-2026-05-14/index.md) plus planned goals 🔗[G108](./Knowledge/Goals/G108-CursorScopedTraversalVisitBitmaps/index.md) and 🔗[G109](./Knowledge/Goals/G109-ListreeReadOnlyMutationSurfaceHardening/index.md): current traversal visited state is already external to slab flags, but per-slab traversal bitmaps would be cleaner and a frozen-tree removal gap must be hardened before trusting shared read-only worker context.
- Reprioritized the open backlog around the full-send slab concept: keep 🔗[G091](./Knowledge/Goals/G091-InternWorkerConcurrencyMvp/index.md) async interns first, pull 🔗[G109](./Knowledge/Goals/G109-ListreeReadOnlyMutationSurfaceHardening/index.md), 🔗[G099](./Knowledge/Goals/G099-InternTaskQualityContracts/index.md), and 🔗[G092](./Knowledge/Goals/G092-CartographerFfiReentrancyMetadata/index.md) forward, and add planned goals 🔗[G103](./Knowledge/Goals/G103-BuildTimeStaticCoreDeclarationImageMvp/index.md)–🔗[G109](./Knowledge/Goals/G109-ListreeReadOnlyMutationSurfaceHardening/index.md) for static declaration images, immutable code frames, read-only slab ownership, Root1 slab advertisement, process-isolated micro-VM interns, traversal bitmaps, and ReadOnly hardening.
- Created/refined 🔗[Layered mmap Micro-VM Architecture](./Knowledge/Concepts/LayeredMmapMicroVmArchitecture/index.md) as the durable concept for static read-only core/import slabs, private VM overlays, optional published communication slabs, process-isolated tiny intern VMs, build-time Root0 static-slab generation, runtime Root1 slab-advertisement/messaging ownership, declarative meta-library import images with lazy process-local native binding, low-upward core vs high-downward micro-VM slab allocation, immortal/static read-only slab ownership, content-addressed core images, capability loader metadata, hybrid JSON-event vs bulk-slab communication, and possible forkserver staging.
- Recorded the primary next implementation path for 🔗[G091 — Intern Worker Concurrency MVP](./Knowledge/Goals/G091-InternWorkerConcurrencyMvp/index.md): add async `intern_start!` / `intern_sync!` using the same ghost-queue polling mechanics as LLM streaming so the coordinator can launch intern jobs, continue work, and poll/drain structured results on the main thread.
- Began 🔗[G091 — Intern Worker Concurrency MVP](./Knowledge/Goals/G091-InternWorkerConcurrencyMvp/index.md): added deterministic raw-Edict `intern_run!` dispatch, which freezes shared context/imports, snapshots input, runs bounded Edict source in a fresh worker VM with private workspace, joins, and returns a structured result copied on the coordinator thread; captured the surface in 🔗[K032 — Edict Intern Worker Surface](./Knowledge/Facts/J3_AgentC/K032_Edict_Intern_Worker_Surface.md).
- Completed 🔗[G080 — LLM REPL Context Management](./Knowledge/Goals/G080-LlmReplContextManagement/index.md): added provider-owned `context_reset!` and `context_inspect!`, launcher REPL slash commands `/reset`/`/clear` and `/context`/`/inspect`, generic raw string equality support for safe command dispatch, regression coverage passing in the 21/21 focused cpp-agent Edict/LLM suite, and captured the durable surface in 🔗[K031 — Edict Provider Object Surface](./Knowledge/Facts/J3_AgentC/K031_Edict_Provider_Object_Surface.md).
- Reorganized Dashboard around the incomplete-goal priority order: G078, G091, G099, G092, G096, G101, G100, G094, G095, G097, G098, G075, G093; completed goals were retired from the open-dashboard view.
- Completed 🔗[G102 — Edict Session ID Startup Flag](./Knowledge/Goals/G102-EdictSessionIdStartupFlag/index.md): added raw Edict `--session`/`--session-id` plus `--session-base` create/resume support under `/tmp/session/<id>/` by default, wired it to the current session-image/slab store, added session id validation and CLI regression coverage, and fixed restore sentinel reservation so post-restore allocations do not use null SlabId `(0,0)`.
- Extracted the user-provided architectural/intern-concurrency summary into planned HRM goals 🔗[G091](./Knowledge/Goals/G091-InternWorkerConcurrencyMvp/index.md)–🔗[G101](./Knowledge/Goals/G101-EdictDirectToolEmissionPath/index.md), covering intern worker MVP, FFI re-entrancy metadata, ReadOnly refinements, curated native cognitive libraries, Edict cognitive scaffolds, mmap resume, composite FFI/logic/speculation demo, architecture WorkProduct preservation, intern quality contracts, Edict isolation hardening, and direct Edict tool emission.

### 2026-05-13
- Completed 🔗[G090 — User-Oriented README](./Knowledge/Goals/G090-UserOrientedReadme/index.md): refocused `README.md` toward potential users by emphasizing AgentC's unique value, target applications, mental model, runnable use patterns, maturity expectations, intern-worker direction, and near/long-term roadmap while removing internal development noise.
- Completed 🔗[G089 — Remove Edict `pop` Keyword](./Knowledge/Goals/G089-RemoveEdictPopKeyword/index.md): removed `pop` from compiler keyword handling and bootstrap builtins, updated Edict code/docs/scripts/tests to prefer `/` and `/ /`, added negative coverage proving bare `pop` is now an ordinary symbol, and validated focused Edict/cpp-agent plus live launcher smoke.
- Completed 🔗[G088 — Refresh README Current State](./Knowledge/Goals/G088-RefreshReadmeCurrentState/index.md): rewrote `README.md` around the current Edict-resident agent control-plane goal, current launcher/modules/provider surface, validation caveats, and intern-worker concurrency direction, replacing the stale "not replacing the outer tool loop" framing.
- Completed 🔗[G087 — Prefer Adjacent Edict Eval Sigil](./Knowledge/Goals/G087-PreferAdjacentEvalSigil/index.md): updated README, Edict/VM guides, modules, scripts, demos, integration snippets, and test source strings to prefer adjacent eval spelling (`word!`), added style notes to the guides, fixed the stale-binary `PiSimulationTest` prompt flood, and validated focused Edict/cpp-agent suites plus launcher smoke.
- Completed 🔗[G086 — Live Google LLM Regression Coverage](./Knowledge/Goals/G086-LiveGoogleLlmRegressionCoverage/index.md): added `EdictLlmModuleTest.LiveGoogleGemmaRequestReturnsOkWhenCredentialsArePresent`, which skips without `GEMINI_API_KEY`/`GOOGLE_API_KEY` and otherwise exercises `llm.init([gemma-4-31b-it])` through the real runtime; `EdictLlmModuleTest.*` passed 9/9 with live `ok`.
- Completed 🔗[G084 — Remove Dead Dictionary Payload Path](./Knowledge/Goals/G084-RemoveDeadDictionaryPayload/index.md) and 🔗[G085 — Split Edict VM Translation Unit](./Knowledge/Goals/G085-SplitEdictVmTranslationUnit/index.md): removed the legacy compiler-side `Dictionary`/`VMEXT_DICT` payload path while preserving JSON object literals, split the VM implementation into core/FFI/bootstrap translation units, rebuilt `edict_tests`, validated focused VM/JSON/rewrite/regression/FFI slices plus `EdictLlmModuleTest.*`, and live-smoked Google/Gemma (`gemma-4-31b-it`) returning `ok`.

### 2026-05-11
- Added 🔗[WP — G074 Real-time FFI Token Streaming](./Knowledge/WorkProducts/WP-G074-RealtimeFFITokenStreaming-2026-05-11/index.md), documenting the decoupled ghost-queue implementation, C ABI/Edict/provider surfaces, Google/Gemma streaming usage, current capabilities, and follow-up edges.
- Completed 🔗[G074 — Real-time FFI Token Streaming](./Knowledge/Goals/G074-RealtimeFFITokenStreaming/index.md): implemented detached provider-worker streaming through `StreamManager`, structured `agentc_stream_sync` JSON envelopes, Edict wrappers/provider `stream_start`/`stream_sync`, Google `streamGenerateContent` SSE parsing, and live-validated `gemma-4-31b-it` returning `ok`.
- Completed 🔗[G079 — Edict Agent Loop Tool Support](./Knowledge/Goals/G079-EdictAgentLoopToolSupport/index.md): added native JSON-envelope file read/write/exact-replace and shell helpers, Edict wrappers plus `agentc_tools`, attached tools as `provider.tools`, and validated direct wrappers plus a curated-launcher provider-context tool flow.
- HRM archival/reassessment pass: retired 9 completed goals (G068, G071, G072, G073, G076, G077, G081, G082, G083) to `LocalContext/Knowledge/Archive/Goals/`, rebuilt Dashboard around 5 active/open goals, marked G074/G075 deferred, kept G078 active, and promoted G079 as the immediate next implementation slice with G080 planned behind it.

### 2026-05-10
- Completed 🔗[G083 — Edict Tail-Prefixed Dictionary History](./Knowledge/Goals/G083-EdictTailPrefixedDictionaryHistory/index.md): fixed leading `-` selectors so `-a`, `@-a`, `/-a`, and `/@-a` operate on the tail/oldest value of dictionary item history; added regression coverage and updated Edict guide docs.
- Completed 🔗[G082 — Edict Prefix Sigil Chain Semantics](./Knowledge/Goals/G082-EdictPrefixSigilChainSemantics/index.md): fixed `/@name` so concatenated no-space prefix sigils apply to the same identifier left-to-right, added non-cleaning `REMOVE_HEAD` for intermediate chain removal, preserved `^name^`, and updated both Edict guide documents plus regression coverage.
- Completed 🔗[G081 — OpenAI Codex Subscription Provider](./Knowledge/Goals/G081-OpenAICodexSubscriptionProvider/index.md): added a native `openai-codex-responses` provider using pi's ChatGPT OAuth credentials, wired Edict `llm.init([openai-codex])`, added preset regression coverage, and live-validated a low-cost `gpt-5.3-codex` SSE request returning `ok`.
- Reviewed and re-ranked the active goalset after landing the first launcher-backed provider REPL surface: formalized 🔗[G079 — Edict Agent Loop Tool Support](./Knowledge/Goals/G079-EdictAgentLoopToolSupport/index.md) and 🔗[G080 — LLM REPL Context Management](./Knowledge/Goals/G080-LlmReplContextManagement/index.md) as the next big work tracks, and demoted 🔗[G074 — Real-time FFI Token Streaming](./Knowledge/Goals/G074-RealtimeFFITokenStreaming/index.md) from active execution to planned status because streaming is not the immediate next seam.
- Extended `edict.sh` no-arg launch into a zero-user-code chat path: with `EDICT_AUTO_CHAT=1` (default) and `EDICT_DEFAULT_PRESET=local-qwen`, the launcher now auto-injects `llm.init([preset]) @provider` plus `provider < repl! > / /`, while `EDICT_AUTO_CHAT=0` preserves the curated raw REPL path.
- Added launcher-focused coverage for that no-arg auto-chat behavior in `cpp-agent/tests/edict_llm_module_test.cpp` and manually verified the cleaned-up mock-runtime launcher output no longer shows the outer bootstrap REPL prompts.
- Added the first launcher-backed `provider.repl()` slice on top of `llm.edict`: `extensions/agentc_stdlib.*` now expose minimal stdin/stdout helpers, `agentc.edict` wraps them as `agentc_read_line_status!` and `agentc_write!`, and provider objects now carry a `repl` thunk intended to run as `provider < repl! > / /`.
- Debugged the new REPL loop in several passes: rebuilt the extensions library after adding the native IO bridge, moved EOF/status packaging into native code as JSON to avoid raw pointer truthiness traps, switched loop recursion from raw self-eval to isolated-call recursion for clean unwind, and fixed repeated-turn corruption by isolating helper-local assignments plus inlining successful response application directly onto `provider.conversation`.
- Added focused `cpp-agent/tests/edict_llm_module_test.cpp` coverage for repeated direct provider requests and launcher-backed `provider.repl()` interaction through `./edict.sh`, then rebuilt and verified `EdictLlmModuleTest.*`, `EdictAgentRootTest.*`, `AgentRootVmOpsTest.*`, `EmbeddedVmRootRestoreTest.*`, `SessionStateStoreTest.*`, and `./edict/edict_tests --gtest_filter='EdictVM.*'` all pass.
- Added a curated AgentC Edict launcher path: new root script `edict.sh` now derives a stable `EDICT_PATH` environment contract, injects an Edict-side `EDICT_PATH` object, preloads `cpp-agent/edict/modules/agentc_curated.edict`, auto-loads the core AgentC Edict modules, configures `llm` bootstrap paths, and then hands off to REPL, raw `-e`, stdin, or file execution.
- Added `cpp-agent/edict/modules/agentc_curated.edict` to centralize boilerplate that had been repeated across demos: it can load `agentc.edict`, `agentc_stateful_loop.edict`, `agentc_provider_contracts.edict`, `agentc_agent_root.edict`, and `llm.edict`, derive an `llm` bootstrap object from `EDICT_PATH`, and configure the `llm` surface in one call via `agentc_curated_init!`.
- Verified the new launcher in eval, stdin, file, and demo-backed flows: `./edict.sh -e 'llm.catalog! to_json! print'`, `printf 'llm.init([local-qwen]) @provider\nprovider.preset_name print\n' | ./edict.sh -`, `./edict.sh tests/cursor_test.edict`, and `./demo_local_llm.sh` all succeeded.
- Simplified the public demos to use the curated launcher instead of repeating import/module/bootstrap setup manually: `demo_local_llm.sh` and `demo_live_llm.sh` now delegate to `./edict.sh`, then issue provider-oriented calls directly.
- Replaced the older `demo_local_llm.sh` stateful-loop demo path with the new `llm.edict` provider-object surface: the script now loads `agentc.edict`, `agentc_stateful_loop.edict`, `agentc_provider_contracts.edict`, and `llm.edict`, configures absolute bootstrap paths via `llm.configure_bootstrap!`, runs `llm.init([local-qwen]) @provider`, and drives the request through `provider < [What is the capital of France?] request! > / /`.
- Re-ran `./demo_local_llm.sh` and verified the local demo now exercises the new Edict-owned LLM surface end to end while still hitting the existing runtime diagnostics/logging path; the emitted canonical request and outbound OpenAI-compatible request remained correct and the response returned `The capital of France is Paris.`
- Added configurable unresolved-lookup modes to the Edict VM at the `REF` boundary: `lax!` preserves symbolic fallback, `strict!` / `strict_null!` now return `null` for missing symbols/paths, and `strict_fail!` returns `null` while also entering failure state with an explicit unresolved-symbol message.
- Extended `edict/tests/vm_execution_test.cpp` with focused regression coverage for bare and dotted strict-null lookup, strict-fail state signaling, and round-tripping back to `lax!`, then rebuilt and verified both `./edict/edict_tests --gtest_filter='EdictVM.*'` and the affected `cpp_agent_tests` Edict/LLM/embedded suites pass.
- Updated 🔗[WP — LLM's Guide to Edict and the VM](./Knowledge/WorkProducts/WP-LlmsGuideToEdictVm-2026-05-10/index.md) and 🔗[G078 — Edict-Resident Agent Loop Consolidation](./Knowledge/Goals/G078-EdictResidentAgentLoopConsolidation/index.md) to record the new strict/lax lookup surface and why it matters for debugging plus `& |`-driven Edict control flow.
- Added 🔗[WP — LLM's Guide to Edict and the VM](./Knowledge/WorkProducts/WP-LlmsGuideToEdictVm-2026-05-10/index.md), a compiler/VM-grounded agent guide documenting Edict term compilation, runtime stack/frame behavior, history-preserving assignment, current truthiness rules, context mutation patterns, and `& |` control-flow guidance.
- Landed the first `llm.init(...)` slice in `cpp-agent/edict/modules/llm.edict`: named presets (`local-qwen`, `local`, `google-gemini-3-1-pro-preview`, `google`), bootstrap override configuration, and stable provider objects with in-place mutating `request` thunks are now available to Edict scripts.
- Refactored the first `llm` design away from closure-regenerated returned-provider objects toward a stateful provider model after confirming key VM behaviors: method-like isolated calls do not mutate sibling fields by default, while `provider < [prompt] request! > / /` reliably mutates the provider object in place.
- Debugged the first `llm` bootstrap path by removing a hanging nested conditional in `llm_ensure_imports`, defaulting bootstrap override state to repo-root paths, and then explicitly carrying `ext` / `runtimeffi` bindings on the provider object so runtime wrapper thunks succeed inside provider-scoped request execution.
- Added `cpp-agent/tests/edict_llm_module_test.cpp`, extended `cpp-agent/CMakeLists.txt`, and updated embedded-VM bootstrap artifacts to load `llm.edict`; rebuilt and verified targeted suites now pass end to end: `EdictLlmModuleTest.*`, `EdictAgentRootTest.*`, `AgentRootVmOpsTest.*`, `EmbeddedVmRootRestoreTest.*`, and `SessionStateStoreTest.*`.
- Advanced 🔗[G078 — Edict-Resident Agent Loop Consolidation](./Knowledge/Goals/G078-EdictResidentAgentLoopConsolidation/index.md) from architecture into the first working provider-contract slice: added `cpp-agent/edict/modules/agentc_provider_contracts.edict`, taught `agentc_agent_root.edict` to build canonical turn requests from `runtime.provider_contract`, and wired the embedded-VM bootstrap to import the new module.
- Added contrasting Edict provider specs/runtime-config constructors for `local` and `google`, plus a temporary C++ `provider_contract_json_for(...)` bridge in `cpp-agent/runtime/persistence/agent_root_vm_ops.cpp` so persisted roots and host-created roots carry the same contract object during bootstrap and rehydration.
- Hardened the mock runtime and embedded-VM tests for the new canonical request shape by accepting `messages[*].content`, extending `EdictAgentRootTest` with provider-contract assertions, and updating embedded-VM/session bootstrap artifacts to load `agentc_provider_contracts.edict`.
- Debugged two Edict integration bugs in the first slice: removed an unsupported pure-Edict string-dispatch attempt (`==`-based provider selection) in favor of explicit `provider_contract` carriage, and fixed both a bracket-balance syntax error in `agentc_provider_contracts.edict` and a root-shape corruption bug in `agentc_agent_root_apply_turn_response`.
- Rebuilt `cpp_agent_tests` and verified the targeted consolidation suites now pass end to end: `EdictAgentRootTest.*`, `AgentRootVmOpsTest.*`, `EmbeddedVmRootRestoreTest.*`, and `SessionStateStoreTest.*`.
- Completed 🔗[G077 — Local LLM Demo Request Diagnostics](./Knowledge/Goals/G077-LocalLlmDemoRequestDiagnostics/index.md): reproduced the failing `./demo_local_llm.sh` path, proved the local server supported streaming, found that `cpp-agent/runtime/common/http_client.cpp` was aborting SSE requests after 30 seconds, and removed that hard stop while adding curl transport diagnostics.
- Extended local runtime debugging visibility by logging the full outbound OpenAI-compatible request in `cpp-agent/runtime/providers/openai/openai_provider.cpp` including URL, headers, and body.
- Fixed request normalization in `cpp-agent/runtime/core/runtime.cpp` so runtime requests accept `messages[*].content` as well as `messages[*].text`; this stopped the demo from sending an empty user prompt to the local model.
- Rebuilt and verified `./demo_local_llm.sh` now sends `{"messages":[{"content":"You are Qwen, created by Alibaba Cloud. You are a helpful assistant.","role":"system"},{"content":"What is the capital of France?","role":"user"}],"model":"qwen","stream":true}` and returns `The capital of France is **Paris**.`
- Added 🔗[G078 — Edict-Resident Agent Loop Consolidation](./Knowledge/Goals/G078-EdictResidentAgentLoopConsolidation/index.md) and 🔗[WP — Edict-Resident Agent Loop Consolidation Plan](./Knowledge/WorkProducts/WP-EdictResidentAgentLoopConsolidation-2026-05-10/index.md) after auditing the current duplication seams across shell demos, Edict request assembly, host bootstrap, runtime normalization, and provider wire adapters.
- Recorded the consolidation direction explicitly: Edict should become the authoritative control plane for root/config/request/UI-loop semantics, the runtime should shrink toward a thinner execution/transport boundary, and the host should remain only the daemon/persistence/attach-detach shell.

### 2026-05-05
- G070 architectural cleanup: Eliminated remaining host-owned state duplicates (`base_runtime_config`, `options.system_prompt`, extra `clear()` calls) from `cpp-agent/main.cpp`. Added `install_vm_agent_root` and `reset_vm_agent_root` to `agent_root_vm_ops.*`. The embedded VM is now the sole live owner of the runtime configuration and session state, resolving the final cleanup goals from the Handoff Note.

### 2026-05-04
- G071 complete: fixed the three interlocking bugs that were blocking `EmbeddedVmRootRestoreTest.FullTurnPersistenceAndResume` — `copy()` signature/cycle-detection fix in `listree.cpp`, `appendBytes`-always-called serialization fix in `listree.h`, and `saveRoot` JSON round-trip fix in `session_state_store.cpp`. All 36 `cpp_agent_tests` now pass.
- Switched to full file-backed allocators for slab storage: inUse arrays now live inside mmap'd slab files alongside item bytes, `configureMmapFileBackedSlabs` auto-enables MmapFile policy, `saveRoot` is now flush+anchor (no copy/serialize), `loadRoot` scans slab directories and reattaches files directly. Removed legacy `session_image_mmap_raw_v1` and `session_image_mmap_structured_attach_v1` formats and all associated code. Fixed `O_CREAT|O_TRUNC` → `O_CREAT|O_EXCL` in `createOwnedSlab` to assert the invariant that a new slab file never collides with an existing one.
- Created 🔗[G072 - Direct Slab Restore Without Full Library Re-import](./Knowledge/Goals/G072-DirectSlabRestoreWithoutReimport/index.md) to investigate bypassing the normalize+reimport cycle on restore by treating Cartographer binding trees as durable and only re-resolving process-local symbol addresses.

### 2026-05-02
- G070 completed: deleted all unneeded legacy C++ agent loop scaffolding (`agent_loop.cpp`, `http_client.cpp`, etc.). Moved `ai_types.h` into the `runtime` tree.
- Removed `agent_root_state.*` and eliminated the JSON trampoline from the normal Edict VM turn path, solidly cementing the embedded VM as the owner of live execution.
- Advanced G071's bootstrap/discovery track by teaching `SessionImageStore` to rediscover allocator slab files directly from allocator directories via slab-header inspection, using that discovery to fill/cross-check slab metadata during load, and proving both native session restore and raw mapped-attach restore still work when manifest allocator entries keep identity/metadata but have empty `slabs` arrays.
- Introduced the first explicit `bootstrap.json` authority layer above that substrate: session images now persist a thin bootstrap record with session identity, root-state location, and allocator identity/metadata records, `SessionStateStore::loadRoot(...)` now prefers bootstrap-based restore with manifest fallback for compatibility, and regression coverage proves full canonical root restore still succeeds after deleting `manifest.json` entirely.
- Fed that substrate back upward into the first explicit runtime rehydration contract slice: `cpp-agent/runtime/persistence/agent_root_vm_ops.*` now exposes `rehydrate_vm_runtime_state(...)`, `cpp-agent/main.cpp` now calls it explicitly on startup and `reset-session`, and new regression coverage proves persisted roots keep only declarative runtime/import rehydration metadata while transient runtime-call scratch keys are cleared.

### 2026-05-01
- Extended the first true allocator-attach implementation slice for 🔗[G071](./Knowledge/Goals/G071-SessionScopedAllocatorImagePersistence/index.md) from raw-byte slabs into the first Listree-participating allocator path: in addition to `session_image_mmap_raw_v1`, the substrate now has `session_image_mmap_structured_attach_v1`, `SessionImageStore` can save/load attachable structured slabs, `Allocator<T>::attachMappedStructuredSlabs(...)` can materialize structured slots directly into mapped slab backing, `ArenaMmapStructuredAttachTraits<agentc::ListreeValueRef>` marks the first safe structured allocator family, and regression coverage now proves mapped `ListreeValueRef` slabs still resolve `SlabId`s and dereference target `ListreeValue`s correctly after attachment.
- Recorded the next step explicitly in 🔗[G071](./Knowledge/Goals/G071-SessionScopedAllocatorImagePersistence/index.md) and 🔗[WP_SessionImagePersistencePlan_2026-05-01](./Knowledge/WorkProducts/WP_SessionImagePersistencePlan_2026-05-01.md): after restore-time mapped attachment, the substrate should now learn file-first slab creation for at least one allocator, with a backing policy that can choose heap-backed or exact-size mmap-file-backed slab growth.
- Added `cpp-agent/tests/run_cpp_agent_tests_with_timeout.sh` so gtests can be validated one-by-one under a shell timeout, and added opt-in Edict startup markers behind `AGENTC_EDICT_TRACE_STARTUP=1` in `edict/main.cpp` and `edict/edict_vm.cpp` for future bootstrap debugging. After rebuilding, all tests now pass under the timeout harness and `ctest -R cpp_agent_tests --output-on-failure` is passing again too.
- Advanced G071 again by landing two adjacent substrate slices: representative raw-byte allocators can now grow file-first slabs via selectable heap-vs-mmap backing policy, and session slab headers now carry allocator name/type identity that can be inspected directly and validated against manifest metadata during mapped/raw restore.
- Continued the transparency push for file-backed allocators by extending file-first mmap-backed slab creation farther through the safe structured/Listree allocator chain: after `CLL<ListreeValueRef>`, `ListreeItem`, and `AATree<ListreeItem>`, the remaining previously-lagging structured `Allocator<T>` participants (`ListreeValue` and `ListreeValueRef`) are now also covered by the general file-backed slab-growth path, with focused tests that reset allocators and remove temporary backing directories after each run.
- Brought `BlobAllocator` into the same file-backed story by adding heap-vs-mmap-file backing for its fixed 64 KiB slabs and covering file-first blob-slab growth plus on-disk byte persistence with cleanup-aware regression tests.
- Recorded that the allocator-conversion push is now broadly complete for the main slab-owning substrate: representative raw-byte allocators, `ListreeValue`, `ListreeValueRef`, `CLL<ListreeValueRef>`, `ListreeItem`, `AATree<ListreeItem>`, and `BlobAllocator` all now have file-first backing coverage, and a blob-backing constructor initialization regression was fixed so ordinary Edict startup remains heap-backed by default.
- Began the first true allocator-attach implementation slice for 🔗[G071](./Knowledge/Goals/G071-SessionScopedAllocatorImagePersistence/index.md): raw-byte slab files now have an attach-friendly mmap format (`session_image_mmap_raw_v1`), `SessionImageStore::loadAllocatorMappedRawSlabs(...)` can return live mapped slab attachments, `Allocator<T>::attachMappedRawSlabs(...)` can bind those mappings directly into a raw allocator's slab table, and new regression coverage proves `SlabId` resolution survives restore through slab-index→mapped-base reconstruction without copying raw slab bytes back onto the heap first.
- Recorded the current file-backed allocator design conception directly in 🔗[G071](./Knowledge/Goals/G071-SessionScopedAllocatorImagePersistence/index.md) rationale so a fresh agent can resume the discussion: keep `SlabId` unchanged, treat mmap addresses as ephemeral, use bootstrap/catalog-driven allocator reconstruction as the stable source of slab-index meaning, and treat filename-derived slab ids as a convenient handshake convention rather than sole authority.
- Added a preliminary bootstrap/restore plan to 🔗[WP_SessionImagePersistencePlan_2026-05-01](./Knowledge/WorkProducts/WP_SessionImagePersistencePlan_2026-05-01.md): bootstrap remains minimal but authoritative, slabs become self-identifying by allocator family + slab id, one representative allocator attach path should be proven first, and VM restore should surface as an explicit lifecycle event rather than a delayed `save()` return.
- Documented allocator lifecycle requirements for true mmap-backed session slabs in 🔗[WP_SessionImagePersistencePlan_2026-05-01](./Knowledge/WorkProducts/WP_SessionImagePersistencePlan_2026-05-01.md) and reduced the mmap load/copy gap one more step by teaching arena slab deserializers to consume mapped payload views directly instead of copying slab payloads into temporary strings first.
- Advanced the G071 slab format toward mmap-backed ownership: session slab files are now written as page-aligned/headered `session_image_mmap_v1` artifacts, manifest entries now record slab format plus payload offset/size metadata, and slab load now uses `mmap` before decode while still preserving load/copy restore semantics.
- Removed the normal native-root JSON trampoline from `SessionStateStore::saveRoot(...)`: save now creates a native checkpointed snapshot via allocator checkpoints + `ListreeValue::copy()` + rollback, then writes manifest-indexed slab images plus a root anchor without disturbing live allocator/VM state; added regression coverage proving the live ambient state survives save.
- Landed the first real G071 session-image substrate slice: added `cpp-agent/runtime/persistence/session_image_store.*`, exported reusable arena metadata/slab/root serialization helpers from `core/alloc.*`, moved `SessionStateStore` onto a manifest/index-driven per-session layout (`manifest.json`, `roots.bin`, `allocators/<name>/...`), and added regression coverage for manifest correctness plus native restore through that substrate.
- Recorded the execution-order decision in project memory: prioritize the new persistence substrate goal 🔗[G071](./Knowledge/Goals/G071-SessionScopedAllocatorImagePersistence/index.md) until the mmap/session restore path is real enough to remove JSON-rematerialization persistence, while keeping only minimal G070/G068 architecture work in flight above it.
- Created substrate goal 🔗[G071](./Knowledge/Goals/G071-SessionScopedAllocatorImagePersistence/index.md) plus 🔗[WP_SessionImagePersistencePlan_2026-05-01](./Knowledge/WorkProducts/WP_SessionImagePersistencePlan_2026-05-01.md) to capture the new lower-level persistence direction: named-session slab images, allocator/type/slab indexing, whole-Listree resurrection, and an mmap-oriented file layout independent of agent-loop policy.
- Added session-isolated persistence layout as groundwork for the mmap-backed slab effort: `SessionStateStore` now stores each session under a named subdirectory, `cpp-agent/main.cpp` accepts `--session` / `AGENTC_SESSION`, and new regression coverage proves separate sessions do not clobber each other's slab/root files.
- Moved the production runtime-call boundary inward for the normal prompt path: `cpp-agent/main.cpp` now uses `run_vm_agent_root_turn_via_imported_runtime(...)`, which bootstraps imported `agentc` runtime bindings inside an embedded VM context and issues the LLM/runtime call from there instead of having the host call `agentc_runtime_request_json(...)` directly.
- Added durable imported-runtime regression coverage for that shift: `cpp-agent/tests/agent_root_vm_ops_test.cpp` now exercises a full VM-owned turn against `cpp-agent/tests/mock_runtime/mock_runtime.cpp`, proving the embedded VM path can import a runtime library, execute the turn, and update canonical root state without host-owned request invocation.
- Extended the first G070 Phase 2 slice into a clearer VM-owned production entrypoint: `cpp-agent/main.cpp` now submits normal prompts through `run_vm_agent_root_turn(...)`, derives reply text from VM-owned root state via `reply_text_from_vm_agent_root(...)`, and the new regression coverage in `cpp-agent/tests/agent_root_vm_ops_test.cpp` now exercises request shaping, full-turn execution via runtime invoker callback, VM-derived reply text, and error handling.
- Landed the first G070 Phase 2 code slice: `cpp-agent/main.cpp` no longer uses `shared_root` for normal prompt turns, new embedded-VM helpers in `cpp-agent/runtime/persistence/agent_root_vm_ops.*` now shape runtime requests and apply runtime responses against the canonical root, and durable regression coverage was added in `cpp-agent/tests/agent_root_vm_ops_test.cpp`.
- Began prosecuting 🔗[G070](./Knowledge/Goals/G070-InvertedLoopSurgicalCleanup/index.md): completed Phase 1 and added 🔗[WP_G070_OwnershipBoundaryMap_2026-05-01](./Knowledge/WorkProducts/WP_G070_OwnershipBoundaryMap_2026-05-01.md), which maps the current production turn path, still-host-owned loop work, and the transient artifact set to rehydrate around restore.
- Updated 🔗[G068](./Knowledge/Goals/G068-ClientAgentSplitEmbeddedVmPersistence/index.md) to explicitly route unfinished Phase 4/5/6 execution through 🔗[G070](./Knowledge/Goals/G070-InvertedLoopSurgicalCleanup/index.md), and recorded more concretely which architectural subparts are complete vs still ahead.
- Added HRM goal 🔗[G070](./Knowledge/Goals/G070-InvertedLoopSurgicalCleanup/index.md) to capture the surgical cleanup plan for the inverted-loop transition: reduce host-owned turn/state mutation, define the transient runtime rehydration boundary, and remove JSON rematerialization from normal native-root persistence.
- Captured the new operator-config slice in project memory and updated the implementation plan: provider/model selection is now treated as config (`agentc-config.json`, `AGENTC_CONFIG`, Edict file/path wrappers), while the next planned implementation step is transient runtime rehydration around embedded VM restore.

### 2026-04-30
- Added a project-root `agentc-config.json`, extended `agentc.edict` with file/path-based config wrappers that can use Edict-native `read_text` + `from_json`, taught the host/demos to prefer config-file-driven runtime selection, and revalidated live restart continuity with `gemini-2.5-flash` without recompiling code.
- Switched the active Google default-model wiring to `gemini-3.1-flash` across host config/defaults, Edict demo modules, and related tests, then confirmed via a live persistence-demo rerun that the current provider path receives `404 NOT_FOUND` for that exact model identifier on `v1beta:generateContent`.
- Extended the persistence slice from canonical-root JSON restore to an embedded VM/root-anchor host path: `SessionStateStore` now exposes `loadRoot(...)` / `saveRoot(...)`, `cpp-agent/main.cpp` constructs an `EdictVM` around the restored anchored root and persists that VM-owned root after turns, and `EmbeddedVmRootRestoreTest` proves a canonical persisted root can seed a fresh VM correctly.
- Moved the runtime-backed host persistence path from transcript-only session JSON to the canonical agent-root object: added `cpp-agent/runtime/persistence/agent_root_state.*`, updated `cpp-agent/main.cpp` to restore/persist and runtime-configure from that root, expanded tests, and fixed the runtime persistence demo script so it validates root restoration cleanly.
- Consolidated the inverted-loop accomplishment stack: the project now has live, tested Edict-native POCs for a hello-world single-step turn, an explicit multi-turn conversation-history loop, and a canonical native agent-root object layered on top of the imported runtime.
- Added `cpp-agent/edict/modules/agentc_agent_root.edict`, `cpp-agent/demo/demo_inverted_agent_root.sh`, and `EdictAgentRootTest`, creating the first canonical native Edict agent-root object with `conversation`, `memory`, `policy`, `runtime`, and `loop` sections and validating it through a live two-turn Gemini run.
- Upgraded `cpp-agent/edict/modules/agentc_stateful_loop.edict`, `cpp-agent/demo/demo_inverted_stateful_loop.sh`, and `EdictStatefulLoopTest` to carry explicit Edict-owned conversation history across turns, producing a live two-turn Gemini demo with a native `messages` list in the final state object.
- Added `cpp-agent/edict/modules/agentc_stateful_loop.edict`, `cpp-agent/demo/demo_inverted_stateful_loop.sh`, and `EdictStatefulLoopTest`, extending the inverted-loop POC from a bare assistant string to a structured single-turn state object containing `system_prompt`, `last_prompt`, `last_response`, and `assistant_text`.
- Added `cpp-agent/edict/modules/agentc_hello_loop.edict`, `cpp-agent/demo/demo_inverted_hello_loop.sh`, and `EdictHelloLoopTest`, creating the first minimal hello-world POC of the inverted loop architecture: Edict now builds the request, calls the runtime, extracts assistant text, and prints a live Gemini response itself.
- Verified the core inversion seam live: Edict directly imported `libagent_runtime.so`, created a runtime handle, made a real Google/Gemini request, and received a normalized assistant response (`"Hello world"`) back through the `agentc_call` wrapper path.
- Added `cpp-agent/runtime/core/provider_registry.*` and moved the active runtime build onto the new registry boundary, then reduced older top-level runtime-support files (`api_registry.*`, `credentials.*`, `http_client.*`, `sse_parser.*`, `providers/google.*`, `providers/openai.*`) to compatibility shims while preserving legacy include paths.
- Added `cpp-agent/runtime/persistence/session_state_store.*` and wired the runtime-backed host to restore/persist shared transcript state through the existing Listree/slab-backed file-store path; added `reset-session` to clear in-memory and persisted state, plus automated `SessionStateStoreTest` coverage.
- Manually validated persistence continuity across host restart with a live Gemini-backed session using `--state-base`: first run answered a prompt, second run restored the prior transcript and answered a follow-up grounded in the previous turn.
- Added automated validation for the new runtime boundary: `cpp_agent_tests` now covers both the raw `libagent_runtime.so` C ABI and the Cartographer-imported Edict wrapper/module path, and passes under direct execution plus `ctest -R cpp_agent_tests`.
- Continued the physical `cpp-agent/` refactor by moving the active runtime build onto `cpp-agent/runtime/common/{credentials,http_client,sse_parser}.*`, `cpp-agent/runtime/core/provider_registry.*`, `cpp-agent/runtime/providers/{google,openai}/...`, and `cpp-agent/runtime/persistence/session_state_store.*` while preserving working host/runtime behavior.
- Refactored `cpp-agent/main.cpp` into a thinner runtime-backed socket host over `libagent_runtime.so`, added `shutdown-agent` behavior plus `--config` / `--socket` / `--provider` / `--model` / `--system-prompt` options, and validated both live Gemini response handling and clean daemon shutdown.
- Added the first Edict-side runtime bridge at `cpp-agent/edict/modules/agentc.edict` plus `cpp-agent/demo/demo_agentc_runtime_edict.sh`, proving the Cartographer-imported `libagent_runtime.so` path works end-to-end and returns normalized JSON envelopes back into Edict values.
- Added `cpp-agent/config/runtime.default.json` as a host/runtime configuration example and validated socket startup from a JSON config file.
- Started implementing the Edict-native runtime split for 🔗[G068](./Knowledge/Goals/G068-ClientAgentSplitEmbeddedVmPersistence/index.md): added `cpp-agent/include/agentc_runtime/agentc_runtime.h`, runtime core/c_api scaffolding under `cpp-agent/runtime/`, a new `agent_runtime` shared-library target producing `libagent_runtime.so`, and built-in provider registration through the new boundary.
- Added formal runtime contracts/work products — 🔗[AgentcRuntimeCAbi](./Knowledge/Contracts/AgentcRuntimeCAbi.md), 🔗[AgentcRuntimeJsonContract](./Knowledge/Contracts/AgentcRuntimeJsonContract.md), and 🔗[WP_CppAgentRuntimeFileStructure](./Knowledge/WorkProducts/WP_CppAgentRuntimeFileStructure.md) — then smoke-tested the new C ABI with Python `ctypes` to verify runtime creation and normalized JSON error return.
- Refined 🔗[G068](./Knowledge/Goals/G068-ClientAgentSplitEmbeddedVmPersistence/index.md) around an Edict-native agent loop: `agentc` becomes an importable Edict capability/module backed by a reusable native LLM runtime library, while the host keeps only transport/lifecycle duties.
- Added 🔗[WP_EdictNativeAgentModuleArchitecture](./Knowledge/WorkProducts/WP_EdictNativeAgentModuleArchitecture.md) as the authoritative plan for the new module/runtime split, normalized JSON→Edict response path, capability gating model, and migration away from a host-owned outer C++ agent loop.
- Completed the LMDB inventory/classification slice for 🔗[G069](./Knowledge/Goals/G069-RemoveLMDBDependencies/index.md) and recorded the removal matrix in 🔗[WP_LMDB_SurfaceArea_Audit_2026-04-30](./Knowledge/WorkProducts/WP_LMDB_SurfaceArea_Audit_2026-04-30.md).
- Confirmed the active LMDB removal surface is concentrated in root CMake, `core/alloc.*`, LMDB-only tests/demo paths, `README.md`, and a bounded set of LocalContext knowledge files.
- Updated `README.md` and key LocalContext persistence knowledge to mark LMDB as legacy/superseded rather than active architecture.
- Removed LMDB-only branches from `tests/alloc_tests.cpp` and `demo/demo_arena_metadata_persistence.cpp`, then rebuilt and revalidated focused memory/file-backed slab persistence tests successfully.
- Finished the active LMDB code/build cleanup: removed `AGENTC_WITH_LMDB` from `CMakeLists.txt`, deleted `LmdbArenaStore` from `core/alloc.h` / `core/alloc.cpp`, rebuilt key targets, and verified active code/build paths contain no LMDB references.
- Added HRM goal 🔗[G068](./Knowledge/Goals/G068-ClientAgentSplitEmbeddedVmPersistence/index.md) to formalize the target runtime architecture: client/agent split, embedded Edict VM, and mmap slab persistence.
- Added architecture work product 🔗[WP_EmbeddedPersistentAgentArchitecture](./Knowledge/WorkProducts/WP_EmbeddedPersistentAgentArchitecture.md) capturing roles, communication boundaries, lifecycle, and restore model.
- Added HRM goal 🔗[G069](./Knowledge/Goals/G069-RemoveLMDBDependencies/index.md) to remove LMDB from the supported build/runtime architecture and make slab persistence the only intended persistence path.

### 2026-05-03
- Archival pass: 1 work product retired to Archive/; Dashboard rebuilt around 2 active goals.
- G072 implementation started: hooked `preload_imported_libraries` into `EdictVM` constructor to naturally restore ASLR-shifted `dlopen` handles from durably restored Listree data without pointers patching.
- G072 implementation started: rewrote `rehydrate_vm_runtime_state` to directly mutate the existing Listree root in-place using `agentc::addNamedItem` instead of making a destructive JSON roundtrip. This prevents non-standard keys like Cartographer schemas and FFI bindings from being wiped out on process restart, keeping slab allocations stable.
- G072 implementation continued: cleaned up legacy JSON normalization code (`normalize_agent_root`, `normalized_conversation`, `replace_vm_root_from_json_or_throw`) from `agent_root_vm_ops.cpp` now that the non-destructive Listree update strategy fully replaces them. Verified all `cpp_agent_tests` pass.
- G072 Phase 2/3 completed: Replaced the transient `EdictREPL` side-channel with direct execution inside the main `EdictVM`.
- G072 Phase 2/3 completed: Replaced legacy string/JSON literal evaluation in `call_runtime_from_vm_or_throw` with direct native Listree stack pushes using `json_value_or_throw`.
- G072 Phase 2/3 completed: Lifted Cartographer dynamic module `resolver.import!` into the main VM lifecycle so the generated bindings become durable members of the session's Listree graph.
- G072 Phase 4 fixed: Fixed Cartographer library path resolution in `preload_imported_libraries`. The module `__cartographer` tags export `"library": "<path>"`, which is now correctly detected and `dlopen`ed on session restore.
- G072 Phase 5 completed: Implemented library-change detection on VM restore.
  - Added `validateLibraryFreshness` to `cartographer/resolver.h` to compare disk file size, mtime, and hash against stored `resolved_*` metadata.
  - `EdictVM::preload_imported_libraries` now calls `validateLibraryFreshness` for each dynamic library and throws `StaleLibraryException` if there is a mismatch.
  - `cpp-agent/main.cpp` catches `StaleLibraryException` and automatically invalidates the `SessionStateStore` (falling back to a cold start from a fresh root).
- G072 Phase 6 completed: Implemented comparative `CompareWarmToColdExecution` regression test. It executes 3 agent turns in a single continuous VM process (Cold) and compares the terminal tree state byte-for-byte against a parallel test that restarts the VM entirely from mmap slabs between every single turn (Warm). The generated listree structures are provably identical.
- Removed legacy JSON serialization methods `load()` and `save()` from `SessionStateStore`, fully transitioning the architecture to raw native `ListreeValue` slab storage. Migrated `session_state_store_test.cpp` to use native APIs.
